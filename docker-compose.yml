version: "3.8"

services:
  backend:
    build: ./backend
    container_name: edu-nova-backend
    expose:
      - "5000"
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    build: ./frontend
    container_name: edu-nova-frontend
    expose:
      - "80"
    restart: always

  nginx:
    build: ./nginx
    container_name: edu-nova-nginx
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend
    restart: always
